# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env.local          # local machine overrides — never commit
.env.*.local        # environment-specific local overrides — never commit
# .env     → committed (safe defaults, no secrets)
# .env.example → committed (documents required variables)
